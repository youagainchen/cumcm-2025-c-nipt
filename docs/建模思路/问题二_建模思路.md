# 问题二最终建模思路：集成风险下的BMI分组与最佳NIPT时点

## 1. 问题目标与最终输出

问题二要求对男胎孕妇按BMI作连续分组，并为每组给出统一NIPT时点，使潜在风险尽可能小，同时分析检测误差的影响。该问题不是一般的聚类问题，而是带有以下结构的决策优化问题：

1. BMI越高，Y染色体浓度通常越低，达到4%阈值所需时间可能越晚；
2. 检测过早可能不达标并需要复检，检测过晚又增加延误发现异常胎儿的风险；
3. 同组孕妇必须采用一个可执行的统一时点；
4. 分组数过多虽可降低样本内风险，却会增加执行复杂度并产生小样本组；
5. 检测误差、概率模型和风险函数参数均存在不确定性，不能把某个小数切点视为精确真值。

本地完整计算后，正式采用3组可执行方案：

| BMI组 | 样本数 | 推荐NIPT时点 |
|---|---:|---:|
| $[20.70,31.5)$ | 138 | 12.35周，约12周+2天 |
| $[31.5,37)$ | 114 | 14.20周，约14周+1天 |
| $[37,46.88]$ | 15 | 17.90周，约17周+6天 |

最终模型的精确3组切点为31.426和36.999。为避免伪精确并便于实施，整理为边界31.5和37。整数边界方案的平均风险为1.910156，与精确3组最优值1.910153几乎无差（增加$3\times10^{-6}$），不改变实质结论。

## 2. 数据口径

主分析单位为267位男胎孕妇。BMI取每位孕妇首次检测时的BMI，因为实际制定初次检测安排时未来BMI不可获得。达标事件定义为事件级平均Y染色体浓度达到或超过4%。

首次达标时间存在三类删失：

- 左删失217人（81.3%）：首次抽血已经达标，只知道真实达标时刻不晚于首次抽血；
- 区间删失43人（16.1%）：观测到一次未达标后转为达标，真实达标时刻位于两次抽血之间；
- 右删失7人（2.6%）：所有观测均未达标，只知道达标时刻晚于末次未达标抽血。

左删失占比很高，因此不能把“首次观测到达标的孕周”直接当成真实达标时间做普通回归，否则会系统性高估达标时间。

## 3. 最终集成风险模型

问题二正式模型是在同一个复检风险递归和同一个动态规划目标中，对两类概率信息对应的期望风险作等权集成。两类信息只是模型输入，不作为独立方案分别汇报。

第一个输入由问题一最终非线性混合效应模型给出单次检测达标概率：

$$p_M(t,b)=P\{Y(t)\ge0.04\mid BMI_0=b\}.$$

第二个输入用于修正首次达标时间的左删失、区间删失和右删失。令$T$为首次达到4%的潜在时间，采用LogNormal AFT组件：

$$\log T=\beta_0+\beta_1(BMI_0-\overline{BMI}_0)+\sigma\varepsilon.$$

其BMI系数为0.03918，95% Wald区间为[0.01024,0.06813]，$p=0.00797$。在复检时使用条件达标概率

$$P(t_{k-1}<T\le t_k\mid T>t_{k-1})=
\frac{F_T(t_k)-F_T(t_{k-1})}{1-F_T(t_{k-1})},$$

避免在每轮复检中重复计算累计概率。将两个组件分别代入相同风险递归，得到$C^{(M)}_{ib}(t)$和$C^{(T)}_{ib}(t)$，最终唯一目标函数为

$$C_{ib}(t)=\tfrac12 C^{(M)}_{ib}(t)+\tfrac12 C^{(T)}_{ib}(t).$$

后续组数选择、BMI切点和检测时点均只根据这个最终目标计算，不再并列展示组件各自的分组结果。

## 4. 风险函数与复检机制

延误风险取连续分段线性函数：

$$R(t)=\begin{cases}
1,&t\le12,\\
1+\dfrac{2(t-12)}{15},&12<t\le27,\\
3+2(t-27),&t>27.
\end{cases}$$

它对应题面的早期低风险、中期高风险和28周后极高风险。连续函数避免阶跃风险把最优解机械地挤到12周边界。

若本次未达标，需要考虑复检及失联：

$$E(t)=p(t)R(t)+[1-p(t)]\{qR_{drop}+(1-q)E(t+\Delta)\}.$$

参数取值为：

- 复检间隔$\Delta=3.7$周，来自115次未达标后再次抽血间隔的中位数，IQR为3.14～4.14周；
- 失败后失联概率$q=0.094$，来自127次未达标检测中12次无后续回访；
- 失联风险$R_{drop}=30$，作为无法获得有效结论的保守代价；
- 初次检测搜索范围为11～25周，不在问题一模型观测域之外外推。

风险函数数值属于价值判断而非自然常数，因此必须配合敏感性分析，不能只报告一组参数下的最优解。

## 5. 连续分组的动态规划

将孕妇按基线BMI从小到大排序。对任意连续区间$[i,j)$，先在候选孕周网格上求该组统一最佳时点：

$$C(i,j)=\min_t\sum_{r=i}^{j-1}E_r(t).$$

再用动态规划求$k$组最优总风险：

$$D(k,j)=\min_i\{D(k-1,i)+C(i,j)\}.$$

切点只能位于不同BMI值之间，每组至少15人。这样同时保证：分组是连续BMI区间、目标直接对应潜在风险、不会把相同BMI拆到不同组，也避免极小样本组。该算法求得全局最优分割，不使用无监督聚类。

## 6. 组数判据

最终集成风险模型的组数结果为：

| k | 平均风险 | 相对不分组降幅 | 新增一组的边际改善 |
|---:|---:|---:|---:|
| 1 | 1.935571 | 0 | - |
| 2 | 1.915161 | 1.0545% | 0.020410 |
| 3 | 1.910153 | 1.3132% | 0.005008 |
| 4 | 1.909168 | 1.3641% | 0.000985 |
| 5 | 1.908881 | 1.3789% | 0.000287 |
| 6 | 1.908616 | 1.3926% | 0.000265 |

组数不由人工指定，而由 `q2_final.py` 的 `select_k()` 依次施加三条可复现判据，取全部满足者中的最小 $k$：

- **C1 收益捕获率**：分组带来的风险下降存在天花板（全部可达收益仅占基线风险的1.3926%），要求该 $k$ 已捕获总可达收益的90%以上；
- **C2 组间时点可辨**：相邻组最优时点间隔须不小于1周，否则两组给出的建议在临床上不构成实际区别，属于无效切分；
- **C3 组样本量**：每组不少于15人。

判据明细如下（完整表见 `outputs/q2_k_criteria.csv`）：

| k | 收益捕获率 | 最小组间时点间隔 | 最小组样本量 | C1 | C2 | C3 |
|---:|---:|---:|---:|:--:|:--:|:--:|
| 2 | 75.72% | 1.85周 | 130 | 否 | 是 | 是 |
| **3** | **94.30%** | **1.85周** | **15** | **是** | **是** | **是** |
| 4 | 97.95% | 0.75周 | 15 | 是 | 否 | 是 |
| 5 | 99.02% | 0.55周 | 15 | 是 | 否 | 是 |
| 6 | 100.00% | 0.55周 | 15 | 是 | 否 | 是 |

$k=2$ 只捕获75.72%的可达收益，未过C1；$k\ge4$ 的相邻组最优时点间隔降至0.75周（约5天）及以下——以 $k=4$ 为例，第1组12.05周与第2组12.80周的建议几乎相同，等于把BMI 20.7–31.77区间的147位孕妇（占55%）切成两组却给出同样的处置，未过C2。故正式选择 $k=3$。

需要说明的是，$k=3$ 相对 $k=4$ 的平均风险仅高0.000985（占基线0.05%），二者在风险数值上几乎无差别；选择 $k=3$ 的主要依据是组间可辨性与方案简约性，而非风险优势。

## 7. 检测误差与参数不确定性

问题一估计的测序内方差为0.000174（平方根Y尺度），约占事件残差方差0.000596的29%。通过去除该分量、采用正式设定和增加同量技术方差进行误差敏感性检查。在3组集成模型中，最高BMI组（第3组）最佳时点均约为18.0周，说明测序误差会改变风险水平，但没有改变最终方案的主要判断。

对LogNormal AFT的全部参数按估计协方差联合Monte Carlo抽样1000次，得到：

| BMI | 80%达标孕周中位数（95%区间） | 90%达标孕周中位数（95%区间） |
|---:|---:|---:|
| 30 | 11.31（10.04～12.75） | 14.37（12.86～16.02） |
| 35 | 13.81（12.14～15.69） | 17.46（15.33～20.07） |
| 40 | 16.86（13.30～21.54） | 21.26（16.50～27.45） |

固定3组边界与时点下，由参数不确定性传播得到的平均风险中位数为1.9178，95%区间为[1.7867,2.0747]。BMI越高，达标时间区间越宽，不能为高BMI孕妇给出过度精确的个体承诺。

## 8. 敏感性分析与结论边界

风险梯度$r_{mid}$是最敏感参数：$r_{mid}=5$时各组均倾向12周检测，分组几乎没有收益；$r_{mid}=2$时高BMI组被推迟到23.3周。失联概率从0.05提高到0.15也会显著推迟高BMI组时点。相反，复检间隔在IQR内变化和测序误差方差变化只造成较小变动。

因此论文应区分两类结论：

- 稳健结论：BMI越高，推荐检测时点总体越晚；分组相对不分组可降低风险，但可达收益上限仅1.39%；测序误差不是主要不稳定来源；
- 条件性结论：边界31.5、37及12.35/14.20/17.90周依赖于最终模型的等权设定和当前风险参数，是透明、可执行的方案，不是普适临床标准。第3组只有15人，应重点进行外部验证。

## 9. 判断模型好坏还需要的数据

当前数据可以完成内部建模，但若要证明方案具有实际预测和决策价值，还需要：

1. 独立地区或独立时间段的孕妇队列，验证不同BMI下的达标概率校准；
2. 10～12周的更密集早期检测，减少81.3%左删失造成的达标时点不确定性；
3. 统一安排的复检数据，降低“何时再抽血”由医生和孕妇选择造成的偏倚；
4. 检测失败原因、样本质量、测序深度和实验批次，用于区分生物学未达标与技术失败；
5. 失联后的真实结局，用于验证$q$和$R_{drop}$，而不是把无回访全部视为高风险；
6. 胎儿异常发现时间、干预方式和结局损失，用真实临床后果校准$R(t)$；
7. 外部验证中的Brier分数、校准曲线、时间依赖AUC、区间覆盖率，以及采用分组方案后的平均风险/复检次数。

其中校准和决策收益比单纯AUC或训练集$R^2$更重要，因为问题二输出的是概率和检测时点决策。

## 10. 正式图表（共3张）

1. 最终风险函数与不同BMI下的集成期望风险；
2. 最终模型的$k=1$～6风险曲线、组数判据明细、3组最佳时点和可执行分组；
3. 最终方案的风险参数、检测误差敏感性和参数不确定性传播。

由`src/q2_plot.py`统一生成，输出到`figures/q2_v1/`，每张同时提供PNG和PDF。

## 11. 复现顺序

```powershell
python src\q2_risk.py
python src\q2_survival.py
python src\q2_optimize.py
python src\q2_final.py
python src\q2_sensitivity.py
python src\q2_plot.py
```

核心输出为：`q2_survival.npy`、`q2_final_k_curve.csv`、`q2_k_criteria.csv`、`q2_exact_groups.csv`、`q2_final_groups.csv`、`q2_sensitivity.csv`和`q2_uncertainty_summary.csv`。
